const pyramidHeight = 4

const printPyramid = (pyramidHeight) => {
    
        for (pyramidLoopTwo = 0; pyramidLoopTwo <= pyramidHeight; pyramidLoopTwo++) {
            console.log('*'.repeat(pyramidLoopTwo));
        
        } 
        
    }

printPyramid(pyramidHeight)